apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vlibras.fullname" . }}-test-connection"
  labels:
{{ include "vlibras.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "vlibras.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
